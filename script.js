document.addEventListener("DOMContentLoaded", () => {
    const orderBtn = document.getElementById("orderBtn");
    orderBtn.addEventListener("click", () => {
        alert("Функция заказа будет подключена позже 🚀");
    });
});
